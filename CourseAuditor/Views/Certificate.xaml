<Window x:Class="CourseAuditor.Views.Certificate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CourseAuditor.Views"
        mc:Ignorable="d"
        Title="Certificate" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <DataGrid>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Ученик" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox>

                                </CheckBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Выбранный">
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel>
                <Button Command="{Binding WriterCertCommand}" x:Name="bAddCertificates" Content="Печка" Width="100">
                    
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
